spring.application.name=demo

# Database Configuration - MySQL
# Update these values with your MySQL database credentials
# The database will be created automatically when the app starts (createDatabaseIfNotExist=true)
spring.datasource.url=jdbc:mysql://localhost:3306/wheelymammoth?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=_&t4y4N*.'jpTh
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA/Hibernate Configuration
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# H2 Console (for development - can be disabled if using MySQL only)
spring.h2.console.enabled=false
spring.h2.console.path=/h2-console

# File Upload Configuration
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# Static Resources
spring.web.resources.static-locations=classpath:/static/

# OpenAI API Key (optional - set in environment variable or here)
# openai.api.key=your-api-key-here